import express from 'express';
// import { isAuthenticated, isAuthorised } from '../middleware/authMiddleware';

const editorialRouter = express.Router();

// editorialRouter.get("/create",isAuthorised,)
// editorialRouter.post("/save",isAuthorised,)
// editorialRouter.delete("/delete/:videoId",isAuthorised,)

export default editorialRouter;